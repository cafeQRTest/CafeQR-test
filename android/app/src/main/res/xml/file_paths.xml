<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Internal cache: /data/user/0/<package>/cache/... -->
    <cache-path
        name="cache"
        path="." />

    <!-- Internal app files: /data/user/0/<package>/files/... -->
    <files-path
        name="files"
        path="." />

    <!-- External app-specific dirs (optional, but safe) -->
    <external-cache-path
        name="external-cache"
        path="." />

    <external-files-path
        name="external-files"
        path="." />

</paths>
